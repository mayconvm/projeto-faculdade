// angular.module('sbAdminApp', [])
// .filter('datePHP', function(objectDate) {
//   var timeStamp = objectDate.timestamp * 1000;

//   var dateTimeStamp = new Date(timeStamp);

//   return moment(dateTimeStamp).format("D/M/Y");
// });
